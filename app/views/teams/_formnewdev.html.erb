
<%= form_for(@team, url: add_user_to_team_path, method: :patch) do |f| %>
  <%= select("", "user_id", User.all.collect { |user| [ user.full_name, user.id ] },
     {include_blank: "Desenvolvedor"}) %>
    <%= f.submit "ADD" %>
<% end %>
