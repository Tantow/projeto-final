<%= form_with(model: direction, local: true) do |form| %>
  <% if direction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(direction.errors.count, "error") %> prohibited this direction from being saved:</h2>

      <ul>
      <% direction.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<!--
 <%= form_for(@direction, url: add_user_to_team_path, method: :patch) do |f| %>
    <%= f.label "PMO" %>
    <%= select("", "user_id", User.all.collect { |user| [ user.full_name, user.id ] },
       {include_blank: "PMO"}) %>
      <%= f.submit "Adicionar PMO" %>
  <% end %> -->
<!--
  <div class="field">
    <%= form.label :pmo %>
    <%= form.text_field :pmo, id: :direction_pmo %>
  </div>

  <div class="field">
    <%= form.label :office %>
    <%= form.text_field :office, id: :direction_office %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div> -->
<% end %>
