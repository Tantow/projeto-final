<div class="new-dev-container">

	<div class="new-dev">
		<%= form_for(@users, url: add_user_to_direction_path, method: :patch) do |f|  %>
 	 	<%= select("", "user_id", User.all.collect { |user| [ user.full_name, user.id ] },
     			{include_blank: "Diretor"}) %>
	</div>
   	 <%= f.submit "ADD", id: "new-direction-btn" %>
	<% end %>

</div>
